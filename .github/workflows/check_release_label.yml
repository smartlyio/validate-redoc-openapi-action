name: Check Labels

on:
  pull_request:
    branches:
    - main
    types:
    - opened
    - labeled
    - unlabeled
    - synchronize

permissions:
  pull-requests: read

jobs:
  check_labels:
    runs-on: ubuntu-22.04
    steps:
    - uses: smartlyio/check-versioning-action@v5
      with:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
